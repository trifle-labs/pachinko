{
  "name": "pachinko-simulation",
  "version": "1.0.0",
  "description": "A p5.js pachinko simulation",
  "scripts": {
    "start": "live-server --port=3000 --no-browser"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}